# Dependências do pipeline (notebooks). Versões flexíveis; ajuste conforme o ambiente.
# Modelo spaCy: instale à parte com  python -m spacy download pt_core_news_md

pandas>=2.0
numpy>=1.24
spacy>=3.7
duckdb>=0.10           # leitura de parquet com LIST<STRUCT>
fastparquet>=2024.2    # leitura de parquet no ambiente local (alternativa ao DuckDB)
huggingface_hub>=0.23  # download do corpus e da tabela de entidades (GLiNER)
seqeval>=1.2           # avaliação token-level (BIO)
scikit-learn>=1.3      # métricas auxiliares
matplotlib>=3.7        # figuras
openai>=1.0            # cliente compatível p/ a API do LLM (E2)
